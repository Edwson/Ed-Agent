# Design draft — Payments & money movement

**Recipe seed (build set):** PaymentRailSelector, MoneyMovementTracker, FxQuoteTicket, ReconciliationMatch, MandateConsent

**Engine recommendations for the requirement:** PaymentRetryDunning, APRDisclosure, FeeAndConflictDisclosure, FxQuoteTicket, LoanOriginationStepper, PaymentRailSelector

Design judgment stays human (Ed + Figma MCP). The agents propose the component set from the system; the operator approves it at the gate below.

---

**Logic review (Refine):** component set covers every sub-requirement; AI suggestions carry a consent surface + audit hook.
